{
  "name": "EnglishWordsStickersJS",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://fargutvest.github.io/EnglishWordsStickersJS/",
  "dependencies": {
    "gh-pages": "^2.2.0",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-scripts": "0.9.5"
  },
  "devDependencies": {},
  "scripts": {
    "@comment": {
      "Ubuntu": "HTTPS=true PORT=8000 react-scripts start",
      "Windows": "set HTTPS=true && set PORT=8000 && react-scripts start"
    },
    "start": "set HTTPS=true && set PORT=8000 && react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "eject": "react-scripts eject"
  }
}
